#!/bin/bash
msubmenub::opt2() {
  cat <<EOF
This module is used to introduce how to calculate the nonradiative decay rate between defect stats. It provides a method that uses density functional theory to calculate the electro-acoustic coupling coefficients corresponding to all phonon modes.（缺陷非辐射衰减）
EOF

exit 0
}
