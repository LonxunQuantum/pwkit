#!/bin/bash
msubmenud::opt1() {
  cat <<EOF
This module is the interface between PWmat and EMC (Effective mass calculator). EMC implements calculation of the effective masses at the bands extrema using finite difference method (not the band fitting method). There are two versions of the program: written in FORTRAN and Python.（算有效质量）
EOF

exit 0
}
